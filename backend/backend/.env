FRONTEND_URL=http://localhost:5173
# ─── Django base ─────────────────────────────────────────────
DEBUG=True
SECRET_KEY=dev-change-me-$(date +%s)
ALLOWED_HOSTS=localhost,127.0.0.1,eleanore-arkosic-regrettingly.ngrok-free.dev

# ─── CORS/CSRF: metti la porta del tuo frontend dev ─────────
# Vite di solito è 5173, CRA è 3000. Adatta se diverso.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://molbook-web.vercel.app
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
SESSION_COOKIE_SAMESITE = "None"
CSRF_COOKIE_SAMESITE = "None"


# ─── Database (scegline UNO: sqlite o postgres) ─────────────
# SQLite (semplice, zero setup)
DATABASE_URL=sqlite:///db.sqlite3

# Oppure Postgres locale (commenta SQLite e usa questa)
# DATABASE_URL=postgres://user:password@localhost:5432/nomedb

# ─── Email (in locale usa la console, niente credenziali reali) ─
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Se vuoi davvero SMTP locale, allora imposta anche:
EMAIL_HOST          = "smtp.libero.it"
EMAIL_PORT          = 465
EMAIL_USE_SSL       = True              # con Libero usi SSL sulla 465
EMAIL_USE_TLS       = False
EMAIL_HOST_USER     = "SGDevelop@libero.it"
EMAIL_HOST_PASSWORD = "SalvoDevelop1!"
DEFAULT_FROM_EMAIL  = "SGDevelop@libero.it"  # da qui partiranno le mail di allauth e dj-rest-auth

